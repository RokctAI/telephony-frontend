{
  "name": "base_sdk",
  "version": "1.21.0",
  "installs": [
    {
      "from": "src/services",
      "to": "app/services/base"
    },
    {
      "from": "templates/components/custom/app-sidebar.tsx",
      "to": "components/custom/app-sidebar.tsx"
    },
    {
      "from": "templates/components/custom/beta-toggle.tsx",
      "to": "components/custom/beta-toggle.tsx"
    },
    {
      "from": "templates/components/custom/hero.tsx",
      "to": "components/custom/hero.tsx"
    },
    {
      "from": "templates/components/custom/footer-chrome.tsx",
      "to": "components/custom/footer-chrome.tsx"
    },
    {
      "from": "templates/components/custom/header-menu.tsx",
      "to": "components/custom/header-menu.tsx"
    },
    {
      "from": "templates/components/custom/header.tsx",
      "to": "components/custom/header.tsx"
    },
    {
      "from": "templates/components/custom/menu-label.tsx",
      "to": "components/custom/menu-label.tsx"
    },
    {
      "from": "templates/components/custom/landing/hero-config.ts",
      "to": "components/custom/landing/hero-config.ts"
    },
    {
      "from": "templates/components/custom/landing/hero-copy.ts",
      "to": "components/custom/landing/hero-copy.ts"
    },
    {
      "from": "templates/components/custom/landing/hero-form.ts",
      "to": "components/custom/landing/hero-form.ts"
    },
    {
      "from": "templates/components/custom/landing/hero-sections.ts",
      "to": "components/custom/landing/hero-sections.ts"
    },
    {
      "from": "templates/components/custom/landing/footer-chrome-config.ts",
      "to": "components/custom/landing/footer-chrome-config.ts"
    },
    {
      "from": "templates/components/custom/landing/header-menu.ts",
      "to": "components/custom/landing/header-menu.ts"
    },
    {
      "from": "templates/components/custom/landing/testimonials-marquee.tsx",
      "to": "components/custom/landing/testimonials-marquee.tsx"
    },
    {
      "from": "templates/components/custom/landing/landing-config.ts",
      "to": "components/custom/landing/landing-config.ts"
    },
    {
      "from": "templates/components/custom/landing/page-sections.ts",
      "to": "components/custom/landing/page-sections.ts"
    },
    {
      "from": "templates/components/custom/landing/plans-query.ts",
      "to": "components/custom/landing/plans-query.ts"
    },
    {
      "from": "templates/components/custom/landing/site-metadata.ts",
      "to": "components/custom/landing/site-metadata.ts"
    },
    {
      "from": "templates/components/custom/landing-content.tsx",
      "to": "components/custom/landing-content.tsx"
    },
    {
      "from": "templates/app/landing/page.tsx",
      "to": "app/landing/page.tsx"
    },
    {
      "from": "templates/app/lib/site-metadata.ts",
      "to": "app/lib/site-metadata.ts"
    },
    {
      "from": "templates/app/opengraph-image.tsx",
      "to": "app/opengraph-image.tsx"
    },
    {
      "from": "templates/app/twitter-image.tsx",
      "to": "app/twitter-image.tsx"
    },
    {
      "from": "templates/app/brand-icon/route.tsx",
      "to": "app/brand-icon/route.tsx"
    },
    {
      "from": "templates/app/styles/rokct-scroll.css",
      "to": "app/styles/rokct-scroll.css"
    },
    {
      "from": "templates/app/styles/rokct-marquee.css",
      "to": "app/styles/rokct-marquee.css"
    },
    {
      "from": "templates/app/actions/base/landing.ts",
      "to": "app/actions/base/landing.ts"
    },
    {
      "from": "templates/app/actions/base/status.ts",
      "to": "app/actions/base/status.ts"
    },
    {
      "from": "templates/components/custom/nav/nav-user.tsx",
      "to": "components/custom/nav/nav-user.tsx"
    },
    {
      "from": "templates/components/custom/nav/team-switcher.tsx",
      "to": "components/custom/nav/team-switcher.tsx"
    },
    {
      "from": "templates/components/ui/date-range-picker.tsx",
      "to": "components/ui/date-range-picker.tsx"
    },
    {
      "from": "templates/hooks/use-mobile.tsx",
      "to": "hooks/use-mobile.tsx"
    },
    {
      "from": "templates/app/actions/base/admin/content.ts",
      "to": "app/actions/base/admin/content.ts"
    },
    {
      "from": "templates/app/actions/base/admin/settings.ts",
      "to": "app/actions/base/admin/settings.ts"
    },
    {
      "from": "templates/app/actions/base/admin/system.ts",
      "to": "app/actions/base/admin/system.ts"
    },
    {
      "from": "templates/app/admin/content/blogs/page.tsx",
      "to": "app/admin/content/blogs/page.tsx"
    },
    {
      "from": "templates/app/admin/content/faqs/page.tsx",
      "to": "app/admin/content/faqs/page.tsx"
    },
    {
      "from": "templates/app/admin/page.tsx",
      "to": "app/admin/page.tsx"
    },
    {
      "from": "templates/app/admin/settings/app/page.tsx",
      "to": "app/admin/settings/app/page.tsx"
    },
    {
      "from": "templates/app/admin/settings/currencies/page.tsx",
      "to": "app/admin/settings/currencies/page.tsx"
    },
    {
      "from": "templates/app/admin/settings/faqs/page.tsx",
      "to": "app/admin/settings/faqs/page.tsx"
    },
    {
      "from": "templates/app/admin/settings/flutter/page.tsx",
      "to": "app/admin/settings/flutter/page.tsx"
    },
    {
      "from": "templates/app/admin/settings/general/page.tsx",
      "to": "app/admin/settings/general/page.tsx"
    },
    {
      "from": "templates/app/admin/settings/landing/page.tsx",
      "to": "app/admin/settings/landing/page.tsx"
    },
    {
      "from": "templates/app/admin/settings/pages/page.tsx",
      "to": "app/admin/settings/pages/page.tsx"
    },
    {
      "from": "templates/app/admin/settings/privacy/page.tsx",
      "to": "app/admin/settings/privacy/page.tsx"
    },
    {
      "from": "templates/app/admin/settings/social/page.tsx",
      "to": "app/admin/settings/social/page.tsx"
    },
    {
      "from": "templates/app/admin/settings/system/page.tsx",
      "to": "app/admin/settings/system/page.tsx"
    },
    {
      "from": "templates/app/admin/settings/terms/page.tsx",
      "to": "app/admin/settings/terms/page.tsx"
    },
    {
      "from": "templates/app/admin/system/backup/page.tsx",
      "to": "app/admin/system/backup/page.tsx"
    },
    {
      "from": "templates/app/admin/system/info/page.tsx",
      "to": "app/admin/system/info/page.tsx"
    },
    {
      "from": "templates/app/admin/system/languages/page.tsx",
      "to": "app/admin/system/languages/page.tsx"
    },
    {
      "from": "templates/app/admin/system/translations/page.tsx",
      "to": "app/admin/system/translations/page.tsx"
    },
    {
      "from": "templates/app/admin/system/update/page.tsx",
      "to": "app/admin/system/update/page.tsx"
    },
    {
      "from": "templates/components/custom/nav/admin-nav.tsx",
      "to": "components/custom/nav/admin-nav.tsx"
    },
    {
      "from": "templates/components/custom/nav/client-nav.tsx",
      "to": "components/custom/nav/client-nav.tsx"
    },
    {
      "from": "templates/app/manager/layout.tsx",
      "to": "app/manager/layout.tsx"
    },
    {
      "from": "templates/app/manager/page.tsx",
      "to": "app/manager/page.tsx"
    }
  ],
  "dependencies": {
    "date-fns": "^4.1.0",
    "framer-motion": "^12.29.0",
    "lucide-react": "^0.563.0",
    "next-auth": "5.0.0-beta.30",
    "recharts": "^2.15.4",
    "sonner": "^2.0.7"
  },
  "devDependencies": {},
  "integrations": [],
  "requires": [
    "app/actions/handson/control/system/global-settings.ts",
    "app/actions/merchants/shop.ts",
    "app/actions/telemetry/admin/dashboard.ts",
    "app/actions/telemetry/dashboard.ts",
    "app/config/features.ts",
    "app/config/platform.ts",
    "app/layout.tsx",
    "app/lib/client.ts",
    "app/lib/i18n/index.ts",
    "app/lib/session.ts",
    "components/custom/brand-logo.tsx",
    "components/custom/branding.tsx",
    "components/custom/nav/delivery-nav.tsx",
    "components/custom/nav/merchant-nav.tsx",
    "components/custom/session-provider.tsx",
    "components/custom/theme-toggle.tsx",
    "components/ui/alert.tsx",
    "components/ui/avatar.tsx",
    "components/ui/badge.tsx",
    "components/ui/breadcrumb.tsx",
    "components/ui/button.tsx",
    "components/ui/calendar.tsx",
    "components/ui/card.tsx",
    "components/ui/collapsible.tsx",
    "components/ui/dialog.tsx",
    "components/ui/dropdown-menu.tsx",
    "components/ui/input.tsx",
    "components/ui/label.tsx",
    "components/ui/popover.tsx",
    "components/ui/select.tsx",
    "components/ui/separator.tsx",
    "components/ui/sidebar.tsx",
    "components/ui/switch.tsx",
    "components/ui/table.tsx",
    "components/ui/tabs.tsx",
    "components/ui/textarea.tsx",
    "lib/utils.ts"
  ],
  "_comment": {
    "about": "base/nextjs is the shared kernel (ADR-005): unlike domain SDKs whose installable content root is templates/, its content root is src/ (mirroring base/dart/lib/src). Next.js has no package mechanism, so kernel modules reach hosts by file copy: installs points at src/services directly and lands the whole kernel services surface at app/services/base/, imported by hosts and by other SDKs' copied files as '@/app/services/base/...'. Since 1.2.0 it also carries the paas-era shell chrome (sidebar, navs, dashboard/admin home, layout) and the admin settings/CMS surfaces as flat templates/ entries in the one top-level installs list (no app_type persona blocks; admin and manager see similar pages and the page code hides what the other role should not see). Since 1.3.0 the kernel's platform-gateway.ts also carries the tenant/session wrapper the paas-era shell kept in app/lib/paas-gateway.ts (paasCall): the tenant base URL is resolved per request (session site, request host, env) and the session's API credentials go out as the Authorization header, mirroring the Dart kernel's HttpService + TokenInterceptor. Since 1.4.0 it also carries the generic landing hero (components/custom/hero.tsx) with its copy in components/custom/landing/hero-config.ts and the section registry components/custom/landing/hero-sections.ts: a feature SDK contributes a section under the hero input by injecting one line at the registry's // @rokct-sdk-hero-sections-start marker through its manifest integrations (the same contract as the nav and compose-flag markers), and the hero loads registered sections with a dynamic import, so the hero itself stays free of any feature's code. Since 1.5.0 the rest of the landing HOST is generic too - and only the host: app/landing/page.tsx (reads the session through the kernel seam and prefetches the platform's plans via app/actions/base/landing.ts), the client orchestrator components/custom/landing-content.tsx, the section registry components/custom/landing/page-sections.ts and the generic values in components/custom/landing/landing-config.ts (auth URLs, the nav's hero/footer ends, the plans query - no product copy). Ray, 2026-09-03: each home SDK holds its own landing page, the way a Dart home SDK holds its profile screens. So the content sections of a product's landing page live in that product's home SDK, which registers each one with one line at the // @rokct-sdk-page-sections-start marker, `{ id: \"<file>\", load: () => import(\"@/components/custom/<file>\") }`; the orchestrator loads them by dynamic import, renders them in ascending meta.order (hero 0, negative before the hero as overlays) and hands each PageSectionProps (id, urls, session, plans, nav). agent_sdk 1.4.0 registers rokctapp's floating nav, logos, chat, social, features, workflow, pricing, comparison, faq and testimonials sections; a shell with nothing registered renders the hero alone. The host files land on the shell's own paths, so the composer overwrites the shell copies. Since 1.6.0 the hero's copy has a seam too: components/custom/landing/hero-copy.ts is a third one-marker registry, // @rokct-sdk-hero-copy-start, where a home SDK registers a module whose default export is a HeroCopy (any subset of HeroConfig: headlineWords, headlineSuffix, wordIntervalMs, placeholders, logoPlaceholderToken, backgroundImage, trustLine, badges, fallbackHref) with one line, `{ id: \\\"<sdk>-hero\\\", load: () => import(\\\"@/components/custom/landing/<file>\\\") }`; the hero lays it over HERO_CONFIG (hero-config.ts keeps rokctapp's words as the default) and shows no copy while the module loads. Nothing registered renders HERO_CONFIG from the first paint as before, so rokctapp is unchanged; a thin shell such as supacharge gets its own hero words from its home SDK (lms_sdk) instead of editing an installed file that the next compose regenerates. Since 1.7.0 the hero carries no input at all. Ray, 2026-09-08: \"no hero work same way as landing, they need to be injected like generic profile, chat box is for chat related stuff, i think if agent sdk is home it inject that chat\" - so the search-style input, its typewriter placeholders, the submit and the hero-sections results that sat inside hero.tsx moved out to agent_sdk 1.5.0 (components/custom/landing/agent-hero-form.tsx), and in their place the hero renders the FIRST entry of a fourth one-marker registry, components/custom/landing/hero-form.ts (// @rokct-sdk-hero-form-start, `{ id: \"<sdk>-<form>\", load: () => import(\"@/components/custom/landing/<file>\") }`), through a next/dynamic loader so the form is server-rendered with the hero; the module's default export takes HeroFormProps (hero, signupUrl, onFocusChange, onActiveChange, onHeadlineWordsChange). hero-sections.ts stays here (feature SDKs still register sections into it) but the registered form, not the hero, consumes it. Nothing registered renders no form: headline, trust line and badges alone (supacharge until lms_sdk registers its own); rokctapp with agent_sdk 1.5.0 renders the same DOM as before. Since 1.8.0 it also carries the platform scrollbar (Ray, 2026-09-08: \"rokctai_frontend has a unique scroll with yellow color i think each sdk should use it, so it can move to base sdk\"): app/styles/rokct-scroll.css is rokctai_frontend's Custom Scrollbar block (app/globals.css lines 165-189: 6px webkit scrollbar, transparent track, #facc15 thumb, #eab308 on hover, same in .dark) with the values as CSS variables on :root (--rokct-scroll-width, --rokct-scroll-track, --rokct-scroll-thumb, --rokct-scroll-thumb-hover; the yellow is the default, a home SDK rethemes by redefining them). app/landing/page.tsx imports it, so every composed shell has the scroll on /landing with no host edit; a host that wants it on every route adds `@import \"./styles/rokct-scroll.css\";` to its app/globals.css. rokctai_frontend keeps its own identical rules, so it renders the same. Since 1.9.0 the plans query has a seam too: components/custom/landing/plans-query.ts is a fifth one-marker registry, // @rokct-sdk-plans-query-start, where a home SDK registers a module whose default export is a LandingPlansQuery ({cmd, payload}) or null, with one line, `{ id: \"<sdk>-plans\", load: () => import(\"@/components/custom/landing/<file>\") }`; app/actions/base/landing.ts prefetches with loadLandingPlansQuery(), which answers the FIRST entry that loads and falls back to LANDING_CONFIG.plansQuery when nothing is registered, so rokctapp still reads the platform Subscription Plan catalog byte-for-byte as before. It exists because that catalog is the plans on which someone RUNS a rokct app (USD 20/200 tenant plans) - right for rokctai_frontend, wrong for a product whose landing sells to its own end users: supacharge's visitors are learners, and lms_sdk registers the tenant's own learner catalog here. Since 1.11.0 a registered section can also say whether it belongs on the page at all: PageSectionMeta.renders is an optional predicate over PageSectionContext ({plans, session} - the same page facts the section itself renders with), and components/custom/landing-content.tsx asks it ONCE and then both skips the section and drops its meta.nav entries. It exists because a section that hides itself still contributed a floating-nav tick that scrolled nowhere: lms_sdk's pricing section draws nothing when the platform returns no plan rows, so it had to give up its Pricing stop altogether to avoid a dead tick. The stop and the section now come off one answer and cannot disagree. A section that declares no predicate always belongs, so every section registered before this renders and navigates exactly as it did. Since 1.13.0 the HEADER has a seam too, and it is the half of Ray's \"menus in header and footer are not injected\" that a home SDK could not answer on its own: the footer half it can (its own footer section owns that markup - lms_sdk's lms-footer-section.tsx has rendered its link row all along), but components/custom/header.tsx is a `requires` file, the host shell's own, so no SDK may ship it and a home SDK had nowhere to put a header link. components/custom/landing/header-menu.ts is a sixth one-marker registry, // @rokct-sdk-header-menu-start, where a home SDK registers a module whose default export is a HeaderMenu with one line, `{ id: \\\"<sdk>-header-menu\\\", load: () => import(\\\"@/components/custom/landing/<file>\\\") }`; loadHeaderMenu() answers the FIRST entry that loads. A HeaderMenu names `anchors` (SECTION ids, not hrefs) and optional fixed `links`, and components/custom/landing-content.tsx resolves the anchors against the live nav it has already computed - the list that has been through every section's meta.renders predicate - so the label and badge come from the meta.nav entry the home SDK already owns and are never restated, and an anchor whose section did not render is DROPPED rather than linked to nothing (the dead-tick mistake 1.11.0 exists to prevent, which a second hand-written list of hrefs would have reintroduced). The row itself is components/custom/header-menu.tsx (HeaderMenuRow), generic chrome in the footer-chrome.tsx mould: neutral black/white alphas and currentColor only, the badge stated in the declared \"new\"/\"soon\" vocabulary without claiming a palette. With a menu the host pins the header and the row together in one sticky wrapper, so the row needs no knowledge of the host header's height; with NOTHING registered loadHeaderMenu() answers null, the row renders nothing and the host emits the bare <Header> element tree it always did - so rokctai_frontend, whose header carries its own mega menu out of app/config/features.ts, is untouched. Since 1.14.0 base_sdk SHIPS the header (Ray, 2026-09-09: rokct.ai and supacharge.app must use ONE header, the same component, with the menu INSIDE it - links inline on desktop, and on mobile nothing in the bar but a burger): components/custom/header.tsx moved from `requires` to `installs`, so the composer lands it on both shells and the design intent of that requires entry no longer holds. The contract now: base ships the header and the menu partials (components/custom/header-menu.tsx: HeaderMenuNav for the inline desktop list, HeaderMenuList for the stacked mobile panel, HeaderMenuActions for the CTA buttons; HeaderMenuRow stays exported as a thin wrapper nothing renders any more), the menu's WORDS still come from the header-menu.ts registry a home SDK writes into (a HeaderMenu may now also name `groups` - a label opening a dropdown of links or anchors, resolved and dropped by the same rule - and `actions` - CTA buttons; resolveHeaderMenu(menu, nav) answers {items, groups, actions} and resolveHeaderMenuItems is unchanged), and the HOST still owns the brand mark, the wordmark and the theme control (components/custom/brand-logo.tsx, branding.tsx and theme-toggle.tsx - the last newly listed in `requires`, both shells carry it at that path) plus the theme tokens the header paints with (bg-background, border-border, text-foreground, bg-primary), so each shell keeps its own palette in the same chrome. Every menu label - the NEW on Supacharge's Partners, a SOON - is ONE component, components/custom/menu-label.tsx (MenuLabel: bg-primary with text-black, Ray: \"use primary color and text in black\"), used by the header in all three places and meant for any footer link row a home SDK builds from the same nav entries; before this the only badge base painted was header-menu.tsx's neutral black/white HeaderMenuBadge, now gone. A caller that passes none of menuItems/groups/actions gets the registered menu loaded by the header itself (loadHeaderMenu() resolved against its optional `nav` prop, the empty list by default, so fixed links, groups and actions render on rokct.ai's /login, /register, /careers and /status too and anchors only where a nav with the section was given; props win when present), an entry with badge \"soon\" is rendered non-navigable (aria-disabled span, not-allowed cursor), and the hero's rotating word is text-primary instead of the hard-coded text-yellow-400, so a shell's brand colour lives in its --primary token. The header is sticky, not fixed, so no page under it needs a top padding; landing-content.tsx passes the resolved menu straight into <Header> and the 1.13.0 sticky wrapper with the row under the header is gone. Also since 1.14.0 base carries the platform marquee, additive only: app/styles/rokct-marquee.css (the `.rokct-marquee` keyframes that until now existed only as `animate-marquee` in rokctai_frontend's tailwind.config.ts, installed and importable exactly like rokct-scroll.css) and components/custom/landing/testimonials-marquee.tsx (TestimonialsMarquee, agent_sdk's testimonials row DOM with the class strings as overridable props, importing the CSS itself), so Supacharge can inherit rokct.ai's auto-scrolling testimonials from its home SDK without a Tailwind config edit. Since 1.15.0 base carries the LINK-PREVIEW shell too (Ray, 2026-09-09: each home SDK provides its link-preview details, the shell lives in base_sdk). app/layout.tsx is host-owned and in no SDK's installs, which is exactly how one shell came to unfurl with hand-written copy in its layout and the other with a Vercel template's metadataBase and description, and why neither had a 1200x630 preview image. components/custom/landing/site-metadata.ts is a seventh one-marker registry, // @rokct-sdk-site-metadata-start, where a home SDK registers a module whose default export is a SiteMetadataCopy (title, description, tagline; optional siteName, url, keywords, ogImage = a READY-MADE raster preview only, logo = the mark the generated image draws, locale) with one line, `{ id: \"<sdk>-site-metadata\", load: () => import(\"@/components/custom/landing/<file>\") }`; loadSiteMetadata() merges every entry in order over { title: PLATFORM_NAME, siteName: PLATFORM_NAME } and app/lib/site-metadata.ts's buildSiteMetadata(overrides?) turns that into the Next Metadata (metadataBase from NEXT_PUBLIC_SITE_URL or the copy's url, title template `%s — <siteName>`, description, applicationName, keywords, canonical, Open Graph and summary_large_image Twitter cards pointing at the preview). app/opengraph-image.tsx (and app/twitter-image.tsx, the same picture) is the generated 1200x630 card drawn with next/og from the same copy: the registered logo inlined as a data URI, the site name large, the tagline under it, on a dark neutral ground with no font fetch; a registered ogImage that is a real raster is served through it as-is, since Next gives the file-based image priority over config. app/landing/page.tsx exports generateMetadata() from buildPageMetadata() (the page form: title absolute, so the layout's template is not applied twice) so the page anonymous visitors are redirected to unfurls right on its own; the host's app/layout.tsx (now a requires entry) must replace its literal `metadata` with `export const generateMetadata = () => buildSiteMetadata()` for every other route to follow. Since 1.16.0 the generated card can also carry a STILL of the app (Ray, 2026-09-09: the link preview shows a still from the guided tour, not only the wordmark): SiteMetadataCopy gains `still` (a portrait png/jpg/webp, public path or absolute URL) and `stillAnchor` (\"bottom\" by default: the phone's top 72px under the card's top edge, bleeding off the bottom; \"top\": hanging from the top edge with the bottom sheet visible), and app/opengraph-image.tsx draws a two-column card - logo or site name, tagline and host on the left 45%, the still 372px wide in a rounded bezel frame on the right 55% - reading the still's size from its own header (PNG, JPEG, WebP); with no still the single-column card is drawn from exactly the 1.15.0 markup. Since 1.17.0 the shell has a FALLBACK favicon (Ray, 2026-09-09: \"on builds that dont have favicon like supacharge you can make it to take first letter of domain\", and \"that letter should take color of primary color\"): app/brand-icon/route.tsx answers GET /brand-icon?s=<16..512, default 64> with a PNG app tile drawn by next/og - the card's #0b0b0b ground, corners rounded 22%, the first letter of the shown host (since 1.19.0 the request host first when it is a public one, else NEXT_PUBLIC_SITE_URL, else the copy's url - see below; \"www.\" stripped, uppercased; else the site name's first letter, else \"R\") centred at about 62% of the square in the shell's PRIMARY colour - the registered `themeColor`, else the first `--primary:` in the `:root` block of the host's app/globals.css read once per process (shadcn `H S% L%`, hex, rgb(), hsl() and oklch() all understood), else white; a primary under 0.18 relative luminance gets a thin ring of itself at 40% alpha around the tile so the letter still reads - cached for a day. buildSiteMetadata() links to it (64 and 192 as icon, 180 as apple) ONLY as a true fallback: an `icons` override wins outright; a host that ships an icon file Next serves by convention (app/favicon.ico, app/icon.png|svg|ico, app/apple-icon.png, public/favicon.ico - checked on disk at call time, server-side) gets no icons key at all so its file stays the icon; and SiteMetadataCopy gains an optional `icon` (public path or absolute URL) a home SDK registers to replace the generated tile with a real one without touching the host, and `themeColor` for a letter colour other than the theme's. Since 1.18.0 the header's `groups` open as ONE panel again (Ray, 2026-09-09: \"no mega menu anymore\" on rokct.ai is a regression; rokct keeps everything its old header had and Supacharge inherits the same header): components/custom/header-menu.tsx renders the groups as a single desktop trigger - the FIRST group's label - that discloses a panel the width of the bar, anchored under it, with the first group's items as a 300px lead column (no repeated heading) and every other group as a headed column in a grid beside it, the geometry of rokctai_frontend's hand-written mega menu in the shell's theme tokens (bg-background, border-border, text-foreground, text-muted-foreground, MenuLabel for badges); 1.14.0-1.16.0 opened a dropdown per group. HeaderMenuLink gains `description` (one line under the label) and `icon` (a HeaderMenuIcon: box, globe, smartphone, message-square, zap, wrench, file-text - a closed set resolved from lucide-react by name so the header bundles seven glyphs, not the library), both carried onto HeaderMenuItem by resolveHeaderMenu, and an item with either renders as a card (icon box, label, blurb, hover arrow) - rokct.ai's three platform tiles. The mobile panel is unchanged (each group a headed section), and a menu with NO groups renders byte-for-byte as in 1.16.0, so Supacharge is untouched until lms_sdk registers groups. Since 1.19.0 the host the shell SHOWS follows the REQUEST first (resolveDisplayHost in app/lib/site-metadata.ts): the letter on the generated favicon tile and the host line on the generated link-preview card come from the request's x-forwarded-host (first value) else host header - port and a leading \"www.\" stripped, lower-cased - so a white-label or custom domain in front of the same deployment gets its own letter and host line; a request host that is not a public one (localhost, 127.0.0.1, [::1], 0.0.0.0, anything ending .vercel.app, .local or .internal, or none) keeps the CONFIGURED site's host (NEXT_PUBLIC_SITE_URL, else the copy's url) so previews and local runs still show the site they belong to, then the site name. Exported alongside: NON_PUBLIC_HOSTS, NON_PUBLIC_HOST_SUFFIXES, HeaderReader, normaliseHost(), isPublicHost(), requestHost(), siteHost(). Since 1.19.1 the favicon letter's colour is found in ANY :root block of the host's app/globals.css: every :root in the file is scanned in source order, nested ones under @layer base or @media included, and the first block that declares --primary wins (a :root whose selector also names .dark is skipped); 1.17.0-1.19.0 read only the first `:root {`, so rokctai_frontend - an early :root of --foreground-rgb variables, the theme tokens in a second :root under @layer base - drew a white R where Ray ruled (2026-09-09) the letter takes the primary colour. The fallback order is unchanged: the registered themeColor, else that --primary, else white. Since 1.20.0 the kernel answers WHICH TENANT a request host belongs to - the host switch. A tenant that points its own domain at the shell (custom_domain on its Company Subscription at the control site) expects that host to open ITS portal; every other host keeps the storefront. app/services/base/tenant-host-control.ts asks the control site's guest method control.control.api.subscription.resolve_site_by_host(host) -> {site_name} | null (a plain POST to ROKCT_BASE_URL's /api/v1/method/<dotted name>, no credentials, since the control gateway routes only registered control: keys), caches the answer in memory (5 min positive, 60 s negative, ROKCT_TENANT_HOST_TTL_MS / ROKCT_TENANT_HOST_NEGATIVE_TTL_MS; ROKCT_TENANT_HOST_TIMEOUT_MS bounds one lookup; ROKCT_TENANT_HOST_LOOKUP=off disables it) and NEVER throws: a network error, timeout, non-2xx or malformed answer is null (unknown host = storefront), logged once. It never asks for a non-public host (1.19.0's isPublicHost), the configured NEXT_PUBLIC_SITE_URL host or the control host, and a host in the ROKCT_TENANT_HOSTS map answers from the map. resolveTenantSiteForRequest(headers) is the edge-safe middleware entry (x-forwarded-host first value else host, port and www. stripped, lower-cased); auth_sdk 1.7.0's middleware forwards the answer as the x-rokct-tenant-site request header (TENANT_SITE_HEADER) and rewrites / to the paas login for that site. platform-gateway.ts registers the same lookup through setTenantHostResolver at load, so resolveTenantBaseUrl's per-host step resolves a custom domain to its backend with no host wiring (a host's own setTenantHostResolver still replaces it). The host predicate moved into the kernel for this (tenant-hosts.ts: NON_PUBLIC_HOSTS, NON_PUBLIC_HOST_SUFFIXES, HeaderReader, normaliseHost, isPublicHost, requestHost); app/lib/site-metadata.ts re-exports them, so its 1.19.0 surface is unchanged. Also since 1.20.0 a HeaderMenuAction may name an icon (HeaderMenuIcon, which gains \"chrome\" - lucide's own Chrome mark) drawn before its label in both layouts, so rokct's extension button gets its mark back; an action without one renders as before. Since 1.21.0 the home SDK also declares what the header's BRAND slot draws (Ray, 2026-09-09: \"i saw supacharge got a s logo in header, let home sdk declare if it needs logo there or not. supacharge text is the logo right now until i design an icon\"): HeaderMenu gains `brand?: { logo?: \"auto\" | \"none\" | <path>; wordmark?: boolean }` in the same header-menu.ts registry a home SDK already answers. \"none\" draws no image (the wordmark, the host's branding.tsx, is the logo - supacharge until an icon is designed); a path draws that image at 32px; \"auto\" (the default) draws a REAL icon only: the copy's registered `icon` from site-metadata.ts when there is one, else the host shell's own components/custom/brand-logo.tsx - the mark every shell drew before this field existed, so a menu that declares nothing (agent_sdk's) draws exactly what it drew and rokct.ai keeps its logo with no change. The generated /brand-icon letter tile is for the browser tab and the share card only and is NEVER drawn in the header, even when named. resolveHeaderBrand(brand, copy) is the pure rule and loadHeaderBrand() the loader; header.tsx renders the slot through next/dynamic (as hero.tsx renders the form) so the first paint on the server already carries the declared mark and no wrong mark flashes. The \"S\" Ray saw was supacharge-web's own brand-logo.tsx, an asset-free placeholder drawing the platform's first letter, not the generated tile.",
    "app/actions/handson/control/system/global-settings.ts": "host shell (handson control actions)",
    "app/actions/merchants/shop.ts": "commerce/merchants (installed by merchants_sdk; the commerce consolidation PR moved app/actions/paas/shop.ts there)",
    "app/actions/telemetry/admin/dashboard.ts": "core/telemetry (installed by telemetry_sdk, this PR)",
    "app/actions/telemetry/dashboard.ts": "core/telemetry (installed by telemetry_sdk, this PR)",
    "app/config/features.ts": "host shell (feature toggles; hero-config.ts reads them for the store badges)",
    "app/config/platform.ts": "host shell",
    "app/layout.tsx": "host shell root layout (host-owned; no SDK ships it). Since base_sdk 1.15.0 it must export `export const generateMetadata = () => buildSiteMetadata()` (from @/app/lib/site-metadata) in place of a literal `export const metadata = {...}`, passing host-only keys such as icons as overrides, so the shell's title, description, Open Graph and Twitter cards come from the copy the home SDK registers at the // @rokct-sdk-site-metadata-start marker in components/custom/landing/site-metadata.ts rather than from hand-written or template copy in this file (since 1.17.0 `icons` may be left out: a host icon file is served by convention, a registered copy `icon` is linked, and with neither the links point at the generated /brand-icon tile)",
    "app/lib/client.ts": "host shell",
    "app/lib/i18n/index.ts": "host shell",
    "app/lib/session.ts": "host shell session seam (neutral no-session copy in the bare shell; auth_sdk overwrites it with the NextAuth-backed one). The kernel's session.ts reads the session ONLY through this seam, never through app/(auth).",
    "components/custom/brand-logo.tsx": "host shell (brand mark the hero renders)",
    "components/custom/branding.tsx": "host shell (brand wordmark the hero renders)",
    "components/custom/nav/delivery-nav.tsx": "delivery repo (no SDK home in paas-map; stays in the shell until the delivery frontend takes it)",
    "components/custom/nav/merchant-nav.tsx": "commerce/merchants (mapped there in paas-map; installed by that SDK)",
    "components/custom/session-provider.tsx": "host shell; auth_sdk 1.4.0 (Users#93) installs it. app-sidebar.tsx and nav/team-switcher.tsx read useSession() from next-auth/react, which only works under the provider this file mounts, so a shell composing base_sdk WITHOUT auth_sdk must ship a pass-through session-provider.tsx (and the next-auth dependency declared above) or /manager 500s",
    "components/custom/theme-toggle.tsx": "host shell (the light/dark control the header renders; both shells carry it at this path with the same `className` prop - base_sdk 1.14.0 ships the header that renders it, so the toggle became a declared prerequisite)",
    "components/ui/alert.tsx": "host shell",
    "components/ui/avatar.tsx": "host shell",
    "components/ui/badge.tsx": "host shell",
    "components/ui/breadcrumb.tsx": "host shell",
    "components/ui/button.tsx": "host shell",
    "components/ui/calendar.tsx": "host shell (SDK-required keep)",
    "components/ui/card.tsx": "host shell",
    "components/ui/collapsible.tsx": "host shell (SDK-required keep)",
    "components/ui/dialog.tsx": "host shell",
    "components/ui/dropdown-menu.tsx": "host shell",
    "components/ui/input.tsx": "host shell",
    "components/ui/label.tsx": "host shell",
    "components/ui/popover.tsx": "host shell",
    "components/ui/select.tsx": "host shell",
    "components/ui/separator.tsx": "host shell",
    "components/ui/sidebar.tsx": "host shell (SDK-required keep)",
    "components/ui/switch.tsx": "host shell",
    "components/ui/table.tsx": "host shell",
    "components/ui/tabs.tsx": "host shell",
    "components/ui/textarea.tsx": "host shell",
    "lib/utils.ts": "host shell"
  }
}
